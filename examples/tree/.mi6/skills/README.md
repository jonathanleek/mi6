A skill is a folder with a `SKILL.md`. An entry here may also be a symlink to
a folder of skills in a checkout you already have, for example:

	ln -s ~/Documents/git/personal/claude-skills/skills claude-skills

`mi6` looks one level inside such a link.
