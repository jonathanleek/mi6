# Meta

This is the domain for `mi6` and its config. The agent here edits the files
that define its own setup. Run `mi6 build` after editing a layer and check
`mi6 resolve` before assuming a change took.
